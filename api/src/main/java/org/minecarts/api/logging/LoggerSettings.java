package org.minecarts.api.logging;

public class LoggerSettings {

    /**
     * When true uses the {@link AnsiConsole#out} PrintStream 
     * instead of {@link System#out}, to support ANSI escape sequences.
     */
    public static boolean useJansi = true;

}